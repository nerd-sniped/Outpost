# Outpost — rung 1 (self-host) configuration. Copy to .env and fill in.
# Rung 2 (Railway) uses template variables instead of this file.

# --- Forge / GitPDM ---------------------------------------------------------
# Provider must match the token's host. "github" or "generic" are the day-one
# supported paths; generic = any git remote via PAT-in-URL or ambient SSH.
GITPDM_PROVIDER=github
GITPDM_HOST=github.com

# A scoped token used as BOTH the git credential and the host-API identity check.
# GitHub: a fine-grained or classic PAT with `repo` scope. Passed via env on rung 1
# (accepted exposure on a box you own); the gatekeeper moves this to a tmpfs file
# on rung 2. Leave blank to start empty and authenticate from GitPDM's panel.
GITPDM_TOKEN=

# Repo to clone on boot (shallow, depth 20). Optional — omit to land in GitPDM's
# first-run panel and clone/create from there.
GIT_REMOTE_URL=

# --- Container runtime ------------------------------------------------------
# Match these to your host user so bind-mounted files aren't root-owned.
PUID=1000
PGID=1000
TZ=UTC

# Relaunch FreeCAD in-place if it crashes, without restarting the container.
RESTART_APP=true

# --- Tailscale (Phase 2) ----------------------------------------------------
# Only used by the tailscale overlay:
#   docker compose -f docker-compose.yml -f compose.tailscale.yml up -d --build
# The base docker-compose.yml alone stays localhost-only and ignores these.
#
# An auth key from https://login.tailscale.com/admin/settings/keys (ephemeral +
# pre-approved recommended). Leave BLANK to bring the node up interactively — the
# tailscale container prints a one-time login URL to its logs:
#   docker compose logs -f tailscale
TS_AUTHKEY=

# Extra `tailscale up` flags, e.g. --advertise-tags=tag:outpost. Optional.
TS_EXTRA_ARGS=

# Auth boundary marker. The tailscale overlay sets AUTH_MODE=tailscale for you
# (the tailnet is the boundary, no gatekeeper). The gatekeeper overlay (below) sets
# AUTH_MODE=gatekeeper for you. Leave unset here.
# AUTH_MODE=

# --- Gatekeeper (Phase 3) ----------------------------------------------------
# Only used by the gatekeeper overlay:
#   docker compose -f docker-compose.yml -f compose.gatekeeper.yml up -d --build
# This is a local stand-in for rung 2 (Railway, Phase 4): the gatekeeper becomes
# the sole entrypoint, Selkies/healthz are not published, and this hits real
# github.com device-flow endpoints (no callback URL needed).

# A GitHub OAuth App's client ID, with Device Flow enabled. No client secret is
# needed — device flow doesn't use one. Create once at
# https://github.com/settings/developers -> New OAuth App -> check "Enable Device
# Flow" (homepage/callback URLs can be anything; they're unused by device flow).
GITHUB_CLIENT_ID=

# The single GitHub login allowed through the door. Anyone else who completes
# device flow is rejected: no cookie is set, no token is written anywhere.
ALLOWED_GITHUB_USER=

# Encrypts (not just signs) the session cookie. Any random 32+ byte string, e.g.
# `openssl rand -hex 32`. Rotate this to invalidate every outstanding session
# instantly — the panic procedure (see SECURITY.md) if a device is stolen.
SESSION_SECRET=

# The gatekeeper's own listen port. Railway will inject PORT automatically in
# Phase 4; set here only for local testing against compose.gatekeeper.yml.
PORT=8081
